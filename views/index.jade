div#game
  div#score
    span Score: 10
  div#game_board
    canvas#game_board_canvas(width = 780, height = 440)

:coffeescript
  socket = io.connect "ws://#{window.location.host}"
  (new Game).start socket
